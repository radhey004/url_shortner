services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "5173:4173"
    depends_on:
      - server

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      MONGO_URI: mongodb+srv://radheyk0017_db_user:CmF2PU2R5i5ma8Kz@cluster0.cxegbvh.mongodb.net/url_shortener?retryWrites=true&w=majority
      REDIS_URL: redis://redis:6379
      CLIENT_URL: http://localhost:5173
      JWT_SECRET: change-this-in-production
    depends_on:
      - mongodb
      - redis

  mongodb:
    image: mongo:8.2
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    
  redisinsight:
    image: redis/redisinsight:latest
    ports:
      - "5540:5540"
    depends_on:
      - redis

volumes:
  mongo_data: